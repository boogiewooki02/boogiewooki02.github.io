# Kim Dongwook — Portfolio

React + TypeScript + Vite 기반 포트폴리오입니다.

## Local preview

```bash
npm install
npm run dev
```

## GitHub Pages

`npm run build` 결과물인 `dist` 디렉터리를 GitHub Pages에 배포합니다. Vite의 상대 경로 설정이 적용되어 프로젝트 페이지에서도 동작합니다.
